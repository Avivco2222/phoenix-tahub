# Phoenix Talent OS — Frontend production environment
# Copy to phoenix-dashboard/.env.production and fill real values.

# Internal URL the Next.js server uses to reach the FastAPI backend.
# In a single-VM deploy with backend bound to localhost:8010, keep as-is.
BACKEND_INTERNAL_URL=http://127.0.0.1:8010
