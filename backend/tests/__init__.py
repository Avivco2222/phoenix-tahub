"""Test suite for the Phoenix Talent OS backend.

Tests are collected by pytest from `backend/` via the conftest.py at
that level, which prepends the backend directory to sys.path so each
test file can do `from main import app` directly.
"""
