"""Analytics primitives — snapshots, query cache, scoring, insights.

Lives under its own package so callers can do
``from analytics.snapshots import build_snapshots`` once the legacy
``internal_logic`` facade is retired.
"""
